# On-chain Verification

Status: deploy prepared (005A)

Deploy preparation note (005A):

- Deploy script and Mantle Sepolia network config are ready (`contracts/scripts/deploy.ts`, `contracts/hardhat.config.ts`).
- No real deployment has happened yet. The address / transaction / explorer fields below stay as placeholders until 005B real deployment under human approval.

## Contract

Contract name:

```text
ReceiptAnchor
```

Contract address:

```text
not deployed
```

Deployment transaction:

```text
not deployed
```

Explorer link:

```text
not available
```

## Sample Anchor

Receipt hash:

```text
not anchored
```

Anchor transaction:

```text
not anchored
```

Explorer link:

```text
not available
```

